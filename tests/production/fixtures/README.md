# AGI-OS certification fixtures
#
# Realistic indirect-prompt-injection payloads used by
# `tests/production/specs/08-prompt-injection.spec.js`.
# They are inert: every URL is under `.example` (RFC 2606) and every "action"
# is only ever *requested*, never performed by these files.
