apiVersion: v1
kind: Service
metadata:
  name: {{ include "agios.fullname" . }}
  labels:
    {{- include "agios.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agios.selectorLabels" . | nindent 4 }}
